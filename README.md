# K-Pharmacy Layout Studio — 약국 레이아웃·조닝·진열 시뮬레이터

약국의 **평면 배치(Layout) · 조닝(Zoning) · 진열 계획(Planogram)** 을 한 파일에서
설계하고, 3D로 검증하며, 리포트로 산출하는 브라우저 기반 도구입니다.
빌드 과정·서버·외부 런타임 의존성이 없으며, HTML 파일을 브라우저로 열면 바로 동작합니다.

| 파일 | 용도 | 대상 |
| --- | --- | --- |
| `pharmacy-layout-studio.html` | **정식판 (v7.2)** — 전체 기능 | 점포개발·MD·인테리어 실무 |
| `pharmacy-layout-lite.html` | **간략판** — 핵심 기능만 | 현장 약사·가맹 상담·초기 스케치 |

---

## 1. 이번 개정의 배경

기존 v6.1은 평면 편집기로서는 완성도가 높았으나, 두 가지가 실무 의사결정을 지원하지 못했습니다.

1. **3D가 "높이 있는 색 블록"에 그쳤다.** 직교에 가까운 비정통 투영, 회전과 무관하게
   고정된 면 음영, 단(段) 표현이 흰 선 몇 개뿐이어서 *어떤 매대의 몇 단에 무엇이 놓이는지*
   를 판단할 수 없었습니다.
2. **진열대별 배치를 조망할 단위가 없었다.** 단별 분류는 선택된 한 개 매대의
   인스펙터에서만 보였고, 매대를 식별할 코드 체계도, 매장 전체의 진열 계획을
   한 화면에서 비교할 수단도 없었습니다.

아래 개정은 이 두 문제를 축으로 진행했습니다.

---

## 2. 3D 렌더링 전면 재구현

`draw3D()` 를 폐기하고 캔버스 2D 기반의 소형 3D 파이프라인을 새로 작성했습니다.

### 2.1 카메라·투영

| 항목 | v6.1 | v7.0 |
| --- | --- | --- |
| 투영 | 사교(oblique) 유사 변환 | **원근 투영** (fov 44°, 근평면 클리핑) |
| 카메라 | yaw/pitch 슬라이더만 | **구면 궤도(orbit) + 팬 + 돌리**, 주시점 높이 제어 |
| 시점 | 자유 회전 1종 | **아이소 / 정면 / 평면 / 고객 시선(眼高 1,450mm)** 프리셋 |
| 조작 | 드래그 회전, 휠 확대 | 드래그 회전 · **Shift+드래그 팬** · 휠 돌리 · **방향키 미세 회전** · **클릭 선택** |

- 근평면 클리핑(Sutherland–Hodgman)을 구현해 **고객 시선 모드에서 카메라가 집기 사이로
  들어가도 화면이 깨지지 않습니다.**
- 렌더는 `requestAnimationFrame` 으로 병합되어 드래그 중 중복 그리기가 발생하지 않습니다.

### 2.2 음영·가시성

- **후면 제거(backface culling)**: 닫힌 입체의 뒷면을 제거해 폴리곤 수를 절반으로 줄이고
  화가 알고리즘의 정렬 오류를 구조적으로 제거했습니다.
- **램버트 음영 + 반구 앰비언트**: 면 법선 기준으로 계산하므로 회전에 따라 명암이 정상 변합니다.
  (v6.1은 꼭짓점 인덱스별 고정 보정값을 써서 회전해도 음영이 따라오지 않았습니다.)
- **카메라 추종 키 라이트**: 광원이 카메라 방위각을 30° 앞서 따라갑니다. 고정 광원을 쓰면
  회전 시 정면이 어두워져 분류 색상이 읽히지 않는 문제를 해결했습니다.
- **돌하우스 뷰**: 카메라와 매장 사이를 가리는 앞쪽 벽체만 자동 제거하고 안쪽 벽은 유지합니다.
- 바닥 그림자, 1m 바닥 격자, 매장 치수, 나침반, 조닝 색상 범례를 추가했습니다.

### 2.3 진열대 지오메트리 — 핵심 개선

단색 박스를 **실제 곤돌라 구조**로 전개합니다.

```
걸레받이 · 좌우 측판 · 배판(양면형은 중앙 배판) · 단별 선반판 · 최상단 캡
   + 단별 전면 구성 ─┬─ ① 분류 색상 단띠(가격 레일)
                     └─ ② 상품 페이싱 타일
   + 조닝 헤더 띠(매대 상단, 소속 존 색)
```

- **단띠(rail)** 는 각 단 하단에 놓이는 불투명 색 띠로, 해당 단의 중카테고리가 속한
  대카테고리 색을 씁니다. 선반 구조를 가리지 않으면서 단별 분류가 즉시 읽힙니다.
- **골든존(850–1500mm)** 에 걸치는 단은 금색 테두리로 강조됩니다.
- 양면 곤돌라(`shelf`)는 전·후면 모두에 단띠와 페이싱이 생성됩니다.
- 매대 본체는 소속 존 색을 옅게 섞어(78% 화이트 혼합) 칠해, 존 소속과 단별 분류를
  동시에 구분할 수 있습니다.
- **LOD**: 화면 투영 높이가 임계 이하인 단은 페이싱 타일과 라벨을 생략합니다.

### 2.4 라벨

- 매대 헤더(코드 · 이름 · 칸/단/높이)와 단별 라벨(`3단 · 오메가3`)을 분리했습니다.
- **겹침 제거(decluttering)**: 카메라에 가까운 순으로 배치하며 이미 놓인 라벨과
  겹치면 생략합니다. 배경 플레이트와 좌측 색상 바를 둬 어떤 배경에서도 판독됩니다.
- 골든존 단 라벨은 금색 테두리로 구분됩니다.
- **3D에서 매대를 클릭하면 2D 평면과 인스펙터가 동시에 선택**되고, 우측에 단별 계획이
  높이 구간(mm)과 함께 표시됩니다.

### 2.5 렌더 모드

`기본` · `높이 히트맵`(시야 차단 검토: 녹→황→적) · `골격`(와이어프레임)

---

## 3. 진열대별 배치의 명확화

### 3.1 진열대 코드 자동 부여

벽면 근접도로 매대를 분류해 코드를 부여합니다. 참조 플래노그램과 동일한 체계입니다.

| 접두 | 위치 | 정렬 |
| --- | --- | --- |
| `W` | 상단 벽면 | 좌 → 우 |
| `L` / `R` | 좌측 / 우측 벽면 | 위 → 아래 |
| `B` | 하단·입구부 | 좌 → 우 |
| `G` | 중앙 매대(곤돌라) | 위 → 아래, 좌 → 우 |

코드는 평면도 배지, 3D 라벨, 진열 계획표, CSV, 리포트에 일관되게 표기되며
인스펙터에서 수기 수정할 수 있습니다. (상단 툴바 `코드` 또는 자동 배치 시 자동 실행)

### 3.2 단 번호 체계 변경 ⚠️

**v6.1은 최상단을 1단으로 표기**했으나, 매대 실무와 참조 플래노그램은 최하단을 1단으로
셉니다. v7.0은 **표기를 최하단=1단으로 통일**했습니다.

- 내부 데이터 순서(`levelDetails[0]` = 최상단)는 변경하지 않았으므로 **기존 JSON은 그대로
  호환**됩니다. 바뀐 것은 화면 표기뿐입니다.
- 인스펙터·계획표·3D·CSV 모두 동일 기준을 사용하며, 각 단의 실제 높이 구간(mm)을 함께 표시합니다.

### 3.3 진열 계획표 (Planogram) — 신규

`진열 계획표` 버튼 또는 `P` 키.

- 전 매대를 **정면도 카드**로 펼쳐 한 화면에서 비교·수정합니다. 카드마다 코드, 이름,
  칸/단 수, 단별 분류 셀렉트, 분류 완성도가 표시됩니다.
- 골든존 단은 음영 처리됩니다.
- 검색(코드·이름·분류), 정렬(코드順 / 대카테고리順 / **미완성 우선**), 골든존만 보기.
- **단 구성 복사 → 다른 매대에 붙여넣기** (동일 구성 매대 열 일괄 적용).
- `미지정 단 채우기` — 매대의 대표 분류를 빈 단에 일괄 적용.
- `단 구성 제안` — 진열 원칙(골든존=주력, 상단=프리미엄, 하단=기본·대용량)에 따라
  대카테고리 내 중카테고리를 결정적으로 분배합니다. **초안 제안이며 전부 수정 가능합니다.**
- `진열 계획 CSV` — 매대×단 단위 전개표(코드, 단 번호, 높이 구간, 골든존 여부,
  대·중카테고리, 비고, 좌표, 규격)를 UTF-8 BOM으로 내보냅니다.

### 3.4 평면도(2D) 표현 강화

- 매대 상단에 **단별 색상 스트립**을 그려, 확대하지 않아도 각 매대의 단 구성과
  미지정 단을 즉시 식별할 수 있습니다.
- 매대 좌상단 외부에 **코드 배지**를 배치했습니다(도형과 겹치지 않음).
- 매대 본체를 옅게 칠해 색상 스트립이 도드라지도록 조정했습니다.
- 보조 라벨을 `2칸 · 3/5단 분류 · H2,100` 형식으로 바꿔 **분류 완성도를 평면에서 바로 확인**합니다.

### 3.5 인스펙터 단 편집기 재구성

- 단별 색상 칩 · 실제 높이 구간(mm) · 골든존 음영을 함께 표시합니다.
- `▲ ▼` 로 단 간 구성을 교체할 수 있습니다.
- `단 복사` / `붙여넣기` / `현재 분류 전 단 적용` / `비우기`.
- 진열 원칙 가이드를 상시 노출합니다.

---

## 4. 분석·진단 고도화

### 4.1 평가 축 재배분 (총점 100)

| 축 | v6.1 | v7.0 | 비고 |
| --- | --- | --- | --- |
| 동선 | 40 | 34 | 겹침·통로·출입구 |
| 조닝 | 25 | 21 | 분류 누락·존 불일치 |
| **진열** | – | **16** | **신규** |
| 운영 | 20 | 17 | 출입구·계산대·조제·창고 |
| 효율 | 15 | 12 | 점유율·조닝 적용률 |

**진열 축(16점)** = 단 분류 완성도 8 + 골든존 활용 6 + 코드 체계 2 − 중앙 고단 매대 감점(최대 2)

### 4.2 추가 진단 항목

- **단별 분류 미완성** — 미지정 단 비율 60% 미만 시 경고, 해당 매대로 이동 가능
- **골든존 활용 부족** — 골든존 단 중 미배정 비율
- **진열대 코드 미부여**
- **중앙 고단 매대 시야 차단 가능** — 1,800mm 초과 중앙 매대

### 4.3 리포트

- KPI에 **단 분류 완성도**, **골든존 활용률** 추가 (총 7개)
- **진열대별 단 배정** 섹션 신규: 코드·규격·단별 색상 칩 전개·골든존 충족(n/m)
- 인쇄·PDF 및 CSV 내보내기는 기존과 동일하게 동작합니다.

---

## 5. 기타 개선

- 상단 툴바에 `진열 계획표`, 캔버스 도구에 `코드` 버튼 추가
- 단축키: `P` 진열 계획표, `3` 3D 보기 (기존 `A` 진단 / `R` 리포트 / `F` 맞춤 / `G` 격자 유지)
- 신규 매대는 선택한 중카테고리를 전 단에 시드하므로 추가 직후 3D에 바로 반영됩니다
  (기존 JSON을 불러올 때는 저장된 값을 그대로 유지합니다)
- 예시 레이아웃이 코드·단별 분류까지 채워진 상태로 로드되어 기능을 바로 확인할 수 있습니다
- 진단 캐시 키에 코드·높이·단수·칸수를 포함해 재계산 누락을 제거했습니다
- 서로 다른 단 수의 매대 간 단 구성 붙여넣기 시 발생하던 예외를 수정했습니다

---

## 6. 간략판 (`pharmacy-layout-lite.html`)

정식판과 **같은 시각 언어**(존 색상, 골든존, 단띠·페이싱 3D, 코드 체계)를 유지하면서
학습 부담을 최소화한 단일 파일입니다. 외부 폰트·라이브러리 의존이 없습니다.

**포함**

- 매장 규격 입력(가로·세로·천장, 평 환산 자동)
- 매대 6종(벽면 매대 / 양면 곤돌라 / 엔드캡 / 냉장 쇼케이스 / 상담대 / 계산대) 클릭 추가
- 드래그 배치 · 격자 스냅(Alt 10mm) · 휠 확대 · 빈 공간 드래그로 화면 이동
- 대카테고리 6종 · 중카테고리 간소화 목록, 단별 분류 지정
- 진열대 코드 자동 부여(W/L/R/B/G), 조닝 영역 추가
- **3D 미리보기** — 동일 엔진 축약판(원근·후면 제거·카메라 추종 조명·단띠·페이싱·
  겹침 제거 라벨·4종 시점 프리셋·클릭 선택)
- 요약 지표 — 진열대 수, 총 단 수, 집기 점유율, 진열 계획 점수(단 분류 45 + 골든존 30 + 점유율 25)
- PNG · JSON 내보내기, 3D PNG 저장

**제외** (정식판에서 이용) — 도면 배경·축척 보정, 자동 매대열 배치, 초기 설계 제안,
레이어 패널, 겹침·통로 진단, 분석 리포트, 실행 취소, 회전, 주석·치수선, 세부 카테고리 전체 트리

---

## 7. 사용 방법

브라우저(Chrome / Edge / Safari 최신)로 HTML 파일을 직접 엽니다. 설치·서버 불필요.

**정식판 권장 순서**

1. `면적부터 시작` — 평수·형태로 외벽·조닝 초안 생성 (또는 `예시` 로 기능 확인)
2. 좌측에서 매대를 배치하거나 `매대열 자동 배치` 실행
3. 상단 `코드` 로 진열대 코드 부여
4. `진열 계획표`(P) 에서 단별 분류 확정 — `단 구성 제안` 으로 초안 후 수정
5. `3D 보기`(3) 에서 **고객 시선** 시점으로 골든존 배치와 시야 차단 검토
6. `설계 진단`(A) → `분석 리포트`(R) → CSV·PDF 산출

---

## 8. 한계와 주의

- 진단 점수, 골든존 판정, 통로 검토는 **기획 검토용 휴리스틱**입니다. 건축·소방·전기,
  의약품 보관 기준, 장애인 편의시설 등 **법정 기준 판정을 대신하지 않습니다.**
  시공 전 현장 실측과 관할기관 확인이 필요합니다.
- `단 구성 제안` 은 진열 원칙에 따른 **초안 생성기**이며, 실제 매출·재고·계약 조건을
  반영하지 않습니다. 반드시 검토 후 확정하십시오.
- 3D는 계획 검토용 표현이며 실측 렌더링·시공 도면이 아닙니다.
- 데이터는 브라우저 내(메모리 / localStorage)에만 저장됩니다. 보존이 필요하면
  **JSON 내보내기** 를 사용하십시오.
- 정식판은 본문 웹폰트(Paperlogy)를 CDN에서 불러옵니다. 오프라인 환경에서는
  시스템 기본 서체로 대체되며 기능에는 영향이 없습니다.

---

## v7.1 — 실제 조닝 문서 연동 (2026-09-17)

`Pharmacy_Full_Zoning_20260912.pdf` · `Pharmacy_Full_Zoning_Shelf_Operations.xlsx`
(41진열면 · 63모듈 · 315선반 · 2,526품목 · 36브랜드)를 시뮬레이터에서 직접 다루도록 확장했습니다.

### 산출물

| 파일 | 내용 |
| --- | --- |
| `data/kpharmacy-zoning-63.json` | 기준안 전체 — 63모듈·315선반·2,526품목·36브랜드 |
| `data/kpharmacy-zoning-63.layout.json` | 기준안 배치 전용(SKU 제외, 224KB) |
| `data/kpharmacy-zoning-63.optimized.json` | 최적화안 + 변경 이력 `changes[]` 15건 |
| `data/kpharmacy-zoning-63.optimized.layout.json` | 최적화안 배치 전용 |
| `docs/zoning-schema.md` | `kpharmacy.zoning/1.0` 형식 명세 |
| `docs/조닝-분석-및-제안.md` | 분석·최적화 제안 전문 |

### 기능 추가

- **조닝 문서 입출력** — `JSON 열기`가 `kpharmacy.zoning/1.0` 문서를 감지해 63모듈을
  좌표·단·운영상태까지 전개합니다. `조닝 문서` 버튼으로 같은 형식으로 다시 씁니다.
  문서의 주테마·대표 상품군은 가져올 때 분류 체계에 자동 등록됩니다.
- **선반 운영 상태 4종** — 상설 선택 / 교체 전시 / 조건부 확장 / 안내를 단 단위로 관리합니다.
  2D 스트립(`분류색`↔`상태색` 전환), 3D `운영 상태` 모드, 계획표 배지, 리포트 표로 표시됩니다.
- **적재 과부하 진단** — 모듈의 SKU 후보를 상설 선반 수용 추정(1선반 14품목)과 대조해
  과적 배수를 계산합니다. 상설 0단 모듈은 별도 항목으로 분리합니다.
- **단 번호 표기 전환** — `S1 = 최상단`(운영 문서 기준, 기본값) ↔ `S1 = 최하단`(매대 시공 기준).

### ⚠️ v7.0에서 바뀐 것 — 단 번호 표기

v7.0은 참조 이미지(ONNURI 플로어맵)를 근거로 **최하단을 1단**으로 표기했습니다.
그러나 이번 운영 문서(2026-09-12)와 엑셀 선반 ID(`W1/S1`…`W1/S5`)는 **S1 = 최상단**입니다.

두 관례가 실제로 충돌하므로, v7.1은 **어느 한쪽을 고르지 않고 문서가 선언하도록** 바꿨습니다.

- 표기를 `S1 · 최상단` 형태의 **S 표기**로 통일 — 숫자만으로 방향을 추측할 일이 없습니다.
- `project.levelOrder`(`topDown` 기본 / `bottomUp`)로 전환하며, 조닝 문서의
  `meta.levelOrder` 를 따릅니다.
- **데이터 인덱스 0은 항상 물리적 최상단**입니다. 표기만 바뀌므로 기존 JSON은 그대로 호환됩니다.

### 성능

진열 계획표가 315개 선반 전부에 분류 셀렉트를 그리면서 `<option>` 83,000개(DOM 93,519)가
생성되어 **열리는 데 4.9초**가 걸렸습니다. 읽기 전용 칩으로 그리고 클릭한 칸에서만
셀렉트를 만들도록 바꿔 **529ms · DOM 5,475** 로 줄였습니다(9배 / 17배).

### 기준 치수에 대하여

원본은 **집기 유효폭·깊이·높이를 실측하지 않았다고 명시**합니다. 따라서 좌표는
상대 배치를 만족하면서 최소 통로를 확보하도록 역산한 **제안값**이며
`meta.geometryStatus: "assumed"` 로 표시됩니다. `params` 블록만 실측치로 교체하면
전체 좌표가 다시 계산됩니다.

| 항목 | 제안 기준값 |
| --- | --- |
| 매장 | 15,400 × 12,200mm (187.9㎡ · 56.8평) |
| 곤돌라 열 통로 | 1,400mm |
| 벽면–집기 통로 | 1,350–1,700mm |
| 모듈 폭 / 곤돌라 깊이 | 1,300 / 900mm (면당 450) |
| 집기 높이 | 벽면 2,100 · 곤돌라 1,650 · 엔드 1,500mm |

### 분석 요약

| 주테마 | 모듈 | 상설단 | 배정 품목 | 수용 추정 | 충족률 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 더마·세정·선케어 | 19 | 35 | 862 | 490 | **57%** |
| 피부약·상처 보호 | 16 | 34 | 122 | 476 | **390%** |

인접한 동일 규격 곤돌라 **G2(18상설단 / 59품목)와 G5(17상설단 / 558품목)의 부담이 9.5배** 차이입니다.
집기를 옮기지 않고 모듈 2개 용도 전환 + 조건부 12단 상설 전환만으로
더마 충족률이 **57% → 80%**, 과적 모듈이 **8개 → 6개**로 개선됩니다.

전체 근거와 제안은 [`docs/조닝-분석-및-제안.md`](docs/조닝-분석-및-제안.md)를 참조하십시오.

---

## v7.2 — 도면 기반 모델과의 통합 (2026-09-17)

업로드된 도면 기반 모델(`Pharmacy_Layout_Studio_Zoning_v7_1.json`)과 v7.1 기준안을
비교하고 통합했습니다. 전문은 [`docs/조닝-비교-및-통합안.md`](docs/조닝-비교-및-통합안.md).

### ⚠️ v7.1 기준안의 치수 오류

v7.1에서 제시한 매장 15,400 × 12,200mm(56.8평)는 **최소 통로 1,300mm를 만족하도록
역산한 가설**이었습니다. 도면 치수열에서 읽은 실제 크기는
**10,512 × 10,319mm(32.8평)** 로, 제 값이 1.7배 컸습니다.
모듈 폭도 1,300mm로 잡았으나 실제는 **925mm**(피치 3,390 − 2,465)입니다.

도구의 기준값에 현실을 맞춘 것이 잘못이었습니다. 실제 통로는 951·1,000mm로
기준보다 좁고, 그것이 사실입니다. 통합안은 **도면 모델을 기준으로 삼습니다.**

또한 곤돌라 배열은 좌측열 위에서부터 **G4 → G3 → G2 → G1**, 우측열 **G6 → G5** 입니다
(v7.1은 순서를 뒤집어 두었습니다).

### 통합 구성

| 가져온 곳 | 내용 |
| --- | --- |
| 도면 모델 (기준) | 좌표·치수·통로 · 평면도 배경 · 315선반 원본 · 안내/조건부의 빈 칸 · 양면 180° 모델링 |
| v7.1 기준안 (보강) | 카탈로그 2,526품목의 모듈별 배정 수 · 페이싱 기반 수용력 · 운영상태 1급 필드 |

**도면 모델의 좌표·치수·빈 칸은 변경하지 않았습니다.**

### 산출물

| 파일 | 용도 |
| --- | --- |
| `data/kpharmacy-integrated-layout.json` | 시뮬레이터에 바로 올리는 통합안 |
| `data/kpharmacy-zoning-63.integrated.json` | 교환용 `kpharmacy.zoning/1.1` |
| `data/uploaded-drawing-model.json` | 도면 모델 원본 |

### 기능 변경

- **`zoning` 블록 하이드레이션** — 요소에 실린 `sourceShelves[].status`·`sourceId`·
  `productGroup`·`need`·`selectionNotes` 를 편집 필드로 승격합니다.
  도면 모델을 그대로 올려도 운영상태 표시와 적재 진단이 동작합니다.
- **페이싱 기반 수용력** — 고정 14품목/선반을 폐기하고
  `(모듈 실폭 − 측판 60mm) ÷ 포장별 페이싱 폭`으로 계산합니다.
  실폭 925mm가 들어오면서 기존 상수는 40% 과대평가였습니다.
- **라벨 정립** — 180° 회전 집기의 글자가 뒤집히던 문제를 도형은 두고 글자만 되돌려 해결.
- **계획 칸 제외** — 안내·조건부 칸을 분류 완성도 분모에서 제외
  (도면 모델에서 "55%만 분류됨"으로 잘못 잡히던 것을 정정).
- **중복 지적 병합** — 같은 유형 35건이 목록을 채워 중요한 지적을 밀어내던 문제 해결.

### 실측 기준 재계산

| 주테마 | 모듈 | 상설단 | 수용 | 배정 | 충족률 | (v7.1 추정) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 더마·세정·선케어 | 19 | 35 | 350 | 862 | **41%** | (57%) |
| 피부약·상처 보호 | 16 | 34 | 513 | 122 | **420%** | (390%) |

**실측 치수를 넣으니 불균형이 더 벌어집니다.** 동일 규격 인접 곤돌라
**G2(458%)와 G5(41%)의 부담 차이는 11.2배**입니다(v7.1 추정 9.5배).

상설 138단의 총 수용은 1,859품목(1페이싱)이며, 주력에 2~3페이싱을 주면
**실질 1,150~1,340품목** — 카탈로그 2,526품목 중 **약 46~53%만 상설 진열 가능**합니다.
v7.1에서 "55~60%"라고 한 수치는 과대였습니다.

---

## 층 전체 도면 대조 (2026-09-17)

해당 층 건축 평면도와 조닝 모델을 대조했습니다.
전문은 [`docs/층도면-대조-결과.md`](docs/층도면-대조-결과.md).

### 결과 ① 도면 모델의 치수는 정확합니다 (오차 0mm)

951 / 3,390 / 1,000 / 1,366 / 7,520 / 1,502 / 1,490 / 10,512 — 전부 도면과 일치합니다.
곤돌라 열 간격 1,395 / 1,379 / 1,295 / 1,295 / 1,405도 동일합니다.

### 결과 ② ⚠️ 중앙 1,000mm가 통로가 아닐 수 있습니다

건물 그리드는 **X1~X7, 총 31,650mm, 5,400mm 반복**입니다.
약국을 X4~X6(10,800mm)으로 놓으면 내부 10,512mm와의 차이가 288mm — **편측 144mm**로
경량벽 두께에 정확히 맞습니다. 이때 **X5 그리드선은 매장 내 x ≈ 5,256mm**,
즉 중앙 간격(4,756~5,756)의 **정중앙에 편차 0mm로 떨어집니다.**

기둥이 있다면 400mm만 되어도 잔여 통로는 좌우 300mm씩이라 **통행이 불가능**합니다.
이 경우 매장은 좌·우 두 구역으로 나뉘고, 연결은 하단 B열 앞과 상단 W열 앞 **두 곳뿐**이
됩니다. 피부 고민축이 A·B(좌측)와 C·D·E(우측)로 갈려 있어 상담 동선에 직접 영향을 줍니다.

**X5 기둥 유무 확인이 필요합니다.** 확인 전이므로 도면에 기둥 요소를 넣지 않았습니다.

### 결과 ③ 인접 조건

| 방향 | 인접 |
| --- | --- |
| 하단(전면) | 하부 OPEN 아트리움 · 좌우 개방 진입 2개소 (1,502 / 1,490mm) |
| 우측 | 화장실(남) · 승강기 · MIRROR · AV — 상시 통행 코어 |
| 좌측 | 인접 테넌트 · 에스컬레이터/계단(UP·DN) — 층 도착 지점 |
| 상단(후면) | 조제실 · POS 4대 |

신규 브랜드 시험 모듈 **B8이 화장실·승강기에 가장 가까운 끝단**에 있습니다.
통행 빈도는 최고이나 체류는 최단인 자리로, 재배치 논거와 유지 논거가 모두 성립합니다.
통행 데이터 없이 단정할 수 없어 **오픈 후 측정 항목**으로 남겼습니다.

데이터셋에는 `floorContext`(건물 그리드 · 인접 조건 · 검증 항목 V1~V3)를 추가했고,
**좌표·치수는 변경하지 않았습니다.**

---

## 오픈 선정 배치 확인과 브랜드존 (2026-09-17)

전문: [`docs/오픈선정-배치와-브랜드존.md`](docs/오픈선정-배치와-브랜드존.md)

### 확정된 기하

- **X5 기둥 없음** — 중앙 1,000mm는 통로로 확정. 앞서 제기한 좌·우 구역 분리 우려는 해소.
- **하단 좌·우 각 4,000mm 개방 유리문** — `door` 요소로 반영.

### ⚠️ 진열 충전율 약 11% — 이전 "과적" 진단의 정정

이전 회차에 "G5-D-2 5.9배 과적"이라고 한 것은 **카탈로그 1,706건의 후보 배정** 기준이었습니다.
실제 **오픈 선정 326건**이 확정되면서 문제가 **반대로 뒤집혔습니다.**

```
전체 선반 유효폭  259,275mm  /  계획 페이싱 391F × 70mm = 27,370mm  →  약 11%
865mm 선반을 채우려면 12F 필요 · 현재 평균 1.8F · 엔드캡 12개는 전부 4%
```

포장 치수가 367건 전부 미입력이라 70mm는 가정치이나, 50~150mm 어느 값을 넣어도
충전율은 8~23%로 일반적 초도 밀도(60~80%)에 못 미칩니다.

### 브랜드존 — 엔드캡 9개

선정 기준은 ① 전문 채널(제약·의료기기 계열 또는 병의원·약국 전용) ② 소구점 명확성
③ 인지도(별도 점수 없음 — 근거 부재).

| 엔드캡 | 브랜드 | 제조·공급 | 소구점 |
| --- | --- | --- | --- |
| G5-ER | 리쥬비(-넥스·-에스) | 파마리서치 | PDRN 의약품·화장품 구분 |
| G5-EL | 프로캄 | 한미사이언스 | EGF·멜라·아크페어 |
| G2-ER | 케어리브 | 니치반 | 습윤 밴드 |
| G2-EL | 이지덤 | 대웅제약 | 습윤 드레싱 |
| G1-EL | 아크로패스 | 라파스 | 마이크로니들 패치 |
| G4-ER | 고려은단 | 고려은단 | 약국 비타민 |
| G4-EL | 에버콜라겐 | 뉴트리 | 먹는 콜라겐 |
| G3-EL | 마데카파마시아 | 동국제약 | 센텔라·재생 |
| G6-ER | 신신 | 신신제약 | 파스·외용 |

G3-ER·G1-ER·G6-EL은 전문 채널 후보가 없어 **보류**했습니다.
모듈 내 확장 여유 16칸은 **채우지 않았습니다** — 충전율 11%에서는 페이싱 확대 여지가 우선입니다.

### 미해결

- **르디퍼** 4개 SKU 전부가 분류 확인 대기(H) — 분류 확정 전 배치 불가.
- **유리문 4m와 B열(x 1,502~9,022)의 폭이 겹침** — B열이 윈도 진열인지 통행 개구인지 확인 필요.

### 산출물

`data/kpharmacy-brandzone-layout.json` · `data/brandzone-plan.json` · `data/brandzone-plan.csv` · `tools/brandzone_plan.py`
